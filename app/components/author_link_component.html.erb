<%= link_to author_path(@author), class: 'inline-flex items-baseline font-bold' do %>
  <%= helpers.heroicon 'user', options: { class: 'inline align-middle', style: 'height: 0.8em;' } %>
  <%= @author %>
<% end %>
